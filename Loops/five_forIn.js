const myobj = {
    name: "rana",
    age: 20,
    study: "cse (Btech)"
}

for (const key in myobj) {

    // console.log(`${key} : ${myobj[key]}`);

}


// in array:


const languageName = ['js', 'cpp', 'c', 'java', 'py'];
for (const key in languageName) {
   console.log(`${languageName[key]}`)
}